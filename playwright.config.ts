import { createnaveeConfig } from "navee-agent-playwright-config/config";

export default createnaveeConfig({
  // Add your custom playwright configuration overrides here
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: 'http://localhost:3000',
  // },
});
